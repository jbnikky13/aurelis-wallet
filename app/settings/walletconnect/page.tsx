'use client';
import {useState} from 'react';
export default function WalletConnectSettings(){const [id,setId]=useState('');const [saved,setSaved]=useState(false);function save(){if(!id.trim())return;localStorage.setItem('aurelis.walletconnect.projectId',id.trim());setSaved(true)}return <main className="shell"><section className="card form"><p className="eyebrow">AURELIS • WALLETCONNECT</p><h1>Connect dApps</h1><p className="muted">Add your WalletConnect Cloud Project ID to enable the relay integration. The project ID is public configuration; never enter a wallet password or private key here.</p><label>WalletConnect Project ID</label><input value={id} onChange={e=>{setId(e.target.value);setSaved(false)}} placeholder="Project ID" autoComplete="off"/><button className="primary" onClick={save}>Save configuration</button>{saved&&<div className="success">WalletConnect configuration saved locally.</div>}<a href="/dapps">Back to dApps</a></section></main>}
