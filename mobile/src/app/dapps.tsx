import {router} from 'expo-router';import {useEffect,useState} from 'react';import {View,Text,Pressable,TextInput,StyleSheet,Alert} from 'react-native';import {createDappPairing} from '@/src/lib/walletconnect';import {getDappSessions,removeDappSession,type DappSession} from '@/src/lib/dapp';import {C} from '@/src/lib/theme';export default function Dapps(){const [uri,setUri]=useState('');const [sessions,setSessions]=useState<DappSession[]>([]);useEffect(()=>{getDappSessions().then(setSessions)},[]);async function connect(){try{await createDappPairing(uri.trim());Alert.alert('Pairing started','Approve the connection request when it appears.');setUri('')}catch(e){Alert.alert('Connection failed',e instanceof Error?e.message:'Invalid WalletConnect URI.')}}async function remove(id:string){await removeDappSession(id);setSessions(await getDappSessions())}return <View style={s.page}><Text style={s.back} onPress={()=>router.back()}>‹ Back</Text><Text style={s.title}>dApps</Text><Text style={s.muted}>Connect AURELIS to trusted Web3 applications.</Text><TextInput value={uri} onChangeText={setUri} autoCapitalize="none" autoCorrect={false} placeholder="Paste WalletConnect URI" placeholderTextColor={C.muted} style={s.input}/><Pressable style={s.primary} onPress={connect}><Text style={s.primaryText}>CONNECT dAPP</Text></Pressable><Text style={s.section}>CONNECTED</Text>{sessions.length?sessions.map(x=><View key={x.id} style={s.card}><Text style={s.name}>{x.name}</Text><Text style={s.muted}>{x.origin}</Text><Pressable onPress={()=>remove(x.id)}><Text style={s.disconnect}>DISCONNECT</Text></Pressable></View>):<Text style={s.muted}>No connected dApps.</Text>}</View>}const s=StyleSheet.create({page:{flex:1,backgroundColor:C.bg,padding:24,paddingTop:70},back:{color:C.accent,fontSize:16,marginBottom:28},title:{color:C.text,fontSize:36,fontWeight:'800'},muted:{color:C.muted,lineHeight:22,marginTop:8},input:{backgroundColor:C.card,borderColor:C.border,borderWidth:1,borderRadius:14,padding:16,color:C.text,marginTop:22},primary:{backgroundColor:C.accent,padding:18,borderRadius:16,alignItems:'center',marginTop:14},primaryText:{color:C.bg,fontWeight:'900'},section:{color:C.muted,fontSize:11,letterSpacing:1.5,marginTop:35,marginBottom:10},card:{backgroundColor:C.card,borderRadius:16,padding:18,marginTop:10},name:{color:C.text,fontSize:17,fontWeight:'800'},disconnect:{color:C.accent,fontWeight:'900',marginTop:15}});