import {Redirect} from 'expo-router';import {useEffect,useState} from 'react';import {View,ActivityIndicator} from 'react-native';import {hasWallet} from '@/src/lib/wallet';export default function Index(){const [ready,setReady]=useState(false);const [exists,setExists]=useState(false);useEffect(()=>{hasWallet().then(v=>{setExists(v);setReady(true)});},[]);if(!ready)return <View style={{flex:1,justifyContent:'center',backgroundColor:'#080A0F'}}><ActivityIndicator color="#A78BFA"/></View>;return <Redirect href={exists?'/home':'/onboarding'}/>}