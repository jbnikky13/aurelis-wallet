export type WalletConnectConfig={projectId:string;relayUrl?:string;metadata:{name:string;description:string;url:string;icons:string[]}};
export function getWalletConnectConfig():WalletConnectConfig{const projectId=typeof window!=='undefined'?localStorage.getItem('aurelis.walletconnect.projectId')??'':process.env.NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID??'';if(!projectId)throw new Error('WalletConnect Project ID is not configured.');return{projectId,metadata:{name:'AURELIS',description:'AURELIS secure multi-chain wallet',url:typeof window!=='undefined'?window.location.origin:'https://aurelis-wallet.vercel.app',icons:[]}}}
